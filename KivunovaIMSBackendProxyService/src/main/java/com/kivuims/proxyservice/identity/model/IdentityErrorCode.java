package com.kivuims.proxyservice.identity.model;

public enum IdentityErrorCode {
    SERVICE_UNAVAILABLE,
    PARSING_ERROR,
    INVALID_CREDENTIALS,
    UNAUTHORIZED,
    FORBIDDEN,
    NOT_FOUND,
    UNKNOWN_ERROR
}
